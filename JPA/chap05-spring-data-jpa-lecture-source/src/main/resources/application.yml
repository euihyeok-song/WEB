server:
  port: 8888

# db config
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/menudb
    username: root
    passeord: root
  jpa:
    generate-ddl: false
    show-sql: true
    database: mysql
    properties:
      hibernate:
        '[format_sql]': true

# ??? ?? ??? ????
logging:
  level:
    com:
      ohgiraffers:
        springdatajpa: debug


